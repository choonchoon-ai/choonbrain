---
name: second-brain
description: 개인 지식 위키(두 번째 브레인) 운영 스킬. 자료를 raw/에 수집하고 wiki/에 한국어 지식 페이지로 컴파일하며, 위키 기반으로 질문에 답하고, 위키 건강 상태를 점검한다. "수집해줘/정리해줘(ingest)", "내가 아는 것 알려줘(query)", "위키 점검해줘(lint)" 요청 시 사용.
---

# 두 번째 브레인 (LLM Wiki)

Andrej Karpathy의 LLM Wiki 개념을 기반으로 한 개인 지식 관리 시스템이다.
핵심 원칙: **LLM이 위키를 쓰고 유지보수하며, 사람은 읽고 질문한다.**

RAG처럼 질문할 때마다 원문을 검색·합성하는 대신, 자료가 들어올 때(ingest 시점에)
지식을 위키 페이지로 합성해 둔다. 새 자료가 도착하면 요약, 상호 참조, 인덱스를
갱신하여 지식이 시간이 지날수록 축적·복리화된다.

## 디렉터리 구조

```
raw/          → 불변 원본 자료 (주제별 하위 폴더)
wiki/         → 컴파일된 지식 페이지 (주제별 하위 폴더, 1단계만 허용)
  ├── index.md → 전체 목차 (지식 베이스 인덱스)
  └── log.md   → 추가 전용 작업 로그
references/   → 템플릿 모음
SKILL.md      → 이 파일 (스키마 및 운영 규칙)
```

## 언어 규칙

- 위키 본문, 요약, 인덱스는 **한국어**로 작성한다. 기술 용어는 영어를 병기하거나 그대로 쓴다 (예: "어텐션(attention)", "fine-tuning").
- 파일명과 폴더명(slug, topic)은 **영문 kebab-case**로 작성한다 (검색·링크 안정성 목적).
- raw/의 원본은 원문 언어 그대로 보존한다.

## 초기화 규칙

- 첫 ingest 시에만 없는 디렉터리를 생성한다: `raw/`, `wiki/`, `wiki/index.md`(제목 "# 지식 베이스 인덱스"), `wiki/log.md`(제목 "# 위키 로그").
- **Query나 Lint에서는 절대 자동 초기화하지 않는다.** 구조가 없으면 먼저 ingest를 실행하라고 안내한다.

---

## 작업 1: Ingest (수집 및 컴파일)

트리거 예시: "이 글 정리해줘", "이 URL 수집해줘", "이 PDF 브레인에 넣어줘", "오늘 배운 것 기록해줘".
Ingest는 반드시 **두 단계를 함께** 수행한다 (fetch만 하고 끝내지 않는다).

### 1단계: raw/에 원본 보존

1. **자료 획득** — 가능한 도구로 콘텐츠를 가져온다. 접근 불가 시 사용자에게 직접 붙여넣기를 요청한다.
   - 웹 아티클/블로그: 본문 텍스트를 마크다운으로 변환
   - 유튜브/강의: 자막·트랜스크립트 또는 사용자가 제공한 내용을 저장
   - 논문/PDF: PDF 파일을 raw/에 함께 두고, 텍스트 추출본을 같은 이름의 .md로 저장
   - 개인 노트/메모: 사용자가 말하거나 쓴 내용을 그대로 기록
   - **클라우드 세션(폰·웹)의 소스 접근 제약**: 이 환경의 egress 정책은 **좁은 허용
     목록** 기반이다. 목록 밖 호스트는 통째로 차단된다(프록시 `CONNECT 403`,
     WebFetch도 403). 우회하지 말고 보고한다.
     - ✅ 접근됨(2026-07 확인): **GitHub**(github.com·raw.githubusercontent.com),
       **패키지 레지스트리**(pypi·npm·crates 등). 그 외는 대체로 막혀 있다고 가정한다.
     - ❌ 차단됨(2026-07 확인): 일반 웹 대부분 — **YouTube**, **Notion**(notion.site/so),
       **X/Twitter**(및 fxtwitter·vxtwitter 미러), **Tistory** 등 블로그, 심지어
       example.com·위키피디아까지. URL 형식을 바꿔도 동일하게 막힌다.
     - `WebSearch`(내장 도구)는 별도 경로라 제목·메타데이터 확인엔 쓸 수 있으나,
       본문 전체를 못 가져오므로 **원문 Ingest에는 부족**하다.
     - 따라서 GitHub·패키지 외 소스는 사용자에게 **텍스트(스크립트·본문·요점)를
       붙여넣어 달라고 요청**해 개인 노트/원문으로 보존한다. 없는 내용을 지어내지 않는다.
2. **주제 폴더 선택** — 기존 raw/ 하위 폴더를 먼저 훑고, 주제가 가까우면 재사용한다. 정말 새로운 주제일 때만 새 폴더를 만든다.
3. **파일명** — `raw/<topic>/YYYY-MM-DD-descriptive-slug.md`
   - slug: kebab-case, 최대 60자, 원본 제목에서 유도
   - 발행일을 모르면 날짜 접두어 생략, 메타데이터 Published를 "Unknown"으로
   - 파일명 충돌 시 숫자 접미어 (`-2`)
4. **메타데이터 헤더** — 출처 URL, 수집일(오늘), 발행일. `references/raw-template.md` 형식을 그대로 따른다.
5. **원문 보존** — 내용을 고치지 않는다. 포맷 노이즈만 정리하고, 의견·주장을 절대 다시 쓰지 않는다. raw/의 기존 파일은 수정 금지(불변).

### 2단계: wiki/에 컴파일

새 콘텐츠를 평가해 목적지를 정한다:

- **기존 글의 핵심 주제와 일치** → 그 글에 병합. Sources/Raw에 출처 추가, 영향받는 문단 갱신.
- **새로운 개념 도입** → 가장 관련 있는 주제 폴더에 새 글 생성. 파일명은 raw 파일명이 아니라 **개념 이름**을 따른다.
- **여러 주제에 걸침** → 가장 관련 있는 폴더에 두고, 다른 주제의 관련 글에 See Also 상호 참조를 추가.

**충돌 처리:** 새 출처가 기존 내용과 모순되면 삭제하지 말고 출처를 밝혀 이견을 주석으로 남긴다. 같은 글 안의 충돌은 그 글에, 서로 다른 글 사이의 충돌은 양쪽 글에 상호 링크와 함께 기록한다.

글 구조는 `references/article-template.md`를 따른다:
- **Sources**: "저자/기관/매체명 + YYYY-MM-DD" 형식, 세미콜론으로 구분
- **Raw**: raw/ 파일로의 마크다운 링크, 세미콜론 구분, 글 위치 기준 상대 경로 (예: `wiki/<topic>/글.md`에서 `../../raw/<topic>/파일.md`)

### 3단계: 연쇄 갱신 (Cascade)

1. 같은 주제 폴더의 글들 중 새 출처에 실질적으로 영향받는 글을 찾아 갱신한다.
2. `wiki/index.md`에서 다른 주제의 관련 항목을 훑고 실질적 영향이 있으면 갱신한다.
3. 손댄 모든 글의 Updated 날짜를 갱신한다.
4. **아카이브 페이지는 연쇄 갱신 대상이 아니다** — 시점 스냅샷으로 보존한다.

### 4단계: 인덱스·로그 갱신

**`wiki/index.md`** (`references/index-template.md` 형식):
- 손댄 모든 글의 항목을 추가/갱신
- 새 주제 섹션 추가 시 한 줄 설명 포함
- Updated 날짜는 파일 타임스탬프가 아니라 **지식 내용이 마지막으로 바뀐 날**

**`wiki/log.md`** (추가 전용):
```
## [YYYY-MM-DD] ingest | <대표 글 제목>
- Updated: <연쇄 갱신된 글 제목>
```
연쇄 갱신이 없으면 "- Updated:" 줄 생략.

### 5단계: 지식 그래프 갱신

새 글에서 등장한 핵심 개념·주장·통찰·절차·사건·개체를 노드로, 그 사이 관계를 엣지로
`wiki/graph/graph.json`에 추가한다. 스키마와 규칙은 아래 "지식 그래프" 절을 따른다.

### 6단계: 웹 발행

아래 "웹 발행" 절차로 사이트를 재배포한다.

---

## 작업 2: Query (질의)

트리거 예시: "X에 대해 내가 뭘 알지?", "Y 관련 내용 다 요약해줘", "내 위키 기준으로 A와 B 비교해줘".

1. `wiki/index.md`를 읽고 관련 글을 찾는다.
2. 해당 글들을 읽고 답을 합성한다.
3. **훈련 지식보다 위키 내용을 우선한다.**
4. 출처는 프로젝트 루트 기준 상대 경로의 마크다운 링크로 인용한다: `[글 제목](wiki/topic/article.md)`
5. 답변은 대화에만 출력한다. 명시적으로 아카이브 요청이 없으면 파일을 쓰지 않는다.

### 답변 아카이브

사용자가 답변 저장을 요청하면:

1. `references/archive-template.md` 형식으로 새 위키 페이지 작성
2. 링크 경로를 파일 기준 상대 경로로 변환 (`wiki/topic/article.md` → `../topic/article.md` 또는 같은 폴더면 `article.md`)
3. **Sources**: 답변에 인용한 위키 글들의 링크. **Raw 필드는 생략** (합성 콘텐츠이므로)
4. 파일명은 질의 주제를 반영 (예: `transformer-architectures-overview.md`)
5. 가장 관련 있는 주제 폴더에 배치. **항상 새 페이지로 생성, 기존 글에 병합 금지**
6. `wiki/index.md`에 항목 추가, Summary 앞에 "[Archived]" 접두어
7. `wiki/log.md`에 기록: `## [YYYY-MM-DD] query | Archived: <페이지 제목>`

---

## 작업 3: Lint (점검)

트리거 예시: "위키 점검해줘", "브레인 상태 확인해줘".

### 결정적 점검 (자동 수정)

**인덱스 일관성:**
- wiki/에 있는데 인덱스에 없는 글 → "(요약 없음)" 플레이스홀더로 항목 추가. Updated는 글 메타데이터 날짜, 없으면 파일 수정일.
- 인덱스가 가리키는 파일이 없음 → "[MISSING]" 표시만 하고 삭제하지 않는다. 판단은 사용자 몫.

**내부 링크** (본문과 Sources — Raw 필드와 index/log는 제외):
- 대상 파일 없음 → wiki/ 전체에서 같은 이름 검색. 정확히 1개 발견 시 경로 자동 수정, 0개 또는 여러 개면 보고.

**Raw 참조** (Raw 필드의 모든 링크):
- 대상 파일 없음 → raw/ 전체에서 같은 이름 검색. 정확히 1개면 자동 수정, 아니면 보고.

**See Also** (각 주제 폴더 내):
- 명백히 빠진 상호 참조 추가, 삭제된 파일로의 링크 제거.

### 휴리스틱 점검 (보고만, 수정 금지)

- 글 사이의 사실 관계 모순
- 더 새로운 출처로 대체된 낡은 주장
- 출처 간 이견이 있는데 충돌 주석이 없는 곳
- 다른 글에서 들어오는 링크가 없는 고아 페이지
- 빠져 있는 주제 간 상호 참조
- 자주 언급되지만 전용 페이지가 없는 개념
- 아카이브 이후 인용된 원본 글이 크게 바뀐 아카이브 페이지

### 점검 후

`wiki/log.md`에 기록: `## [YYYY-MM-DD] lint | 발견 <N>건, 자동 수정 <M>건`

---

## 지식 그래프 (Knowledge Graph)

`wiki/graph/`에 인터랙티브 3D 지식 그래프가 있다 (`index.html` 뷰어 + `graph.json` 데이터).
뷰어는 손대지 않아도 되고, **Ingest 때마다 `graph.json`만 갱신**하면 된다.

`graph.json` 스키마:
- `nodes[]`: `{ id, label, type, origin, topic, tags[], url, summary }`
  - `type`: `concept`(개념/의미) · `insight`(통찰) · `claim`(주장) · `procedure`(절차) · `event`(사건) · `entity`(개체)
  - `origin`: `external`(외부 자료에서 온 사실) · `mine`(위키가 합성한 통찰·교차연결)
  - `topic`: 해당 wiki 주제 폴더명. 여러 주제를 잇는 노드는 `cross`.
  - `url`: 그래프 페이지(`/graph/`) 기준 상대 경로. 보통 `../<topic>/<slug>/` (해당 wiki 글). cross는 `../`.
  - `summary`: 한 줄 설명(한국어).
- `links[]`: `{ source, target, type }`
  - `type`: `support`(지지) · `extend`(확장) · `refute`(반박) · `related`(관련) · `part-of`(포함)

규칙:
- 새 글의 개념을 노드로 추가하고, **기존 노드와의 관계를 엣지로 잇는다**(특히 다른 토픽과의 연결).
- 토픽을 가로지르는 통찰은 `origin:"mine"`, `topic:"cross"` 노드로 만들어 양쪽을 연결한다.
- id는 짧고 고유하게(예: `km-llm-wiki`). 중복 금지.
- 표준 JSON(주석 없음)을 유지한다.

## 웹 발행 (Deploy)

이 위키는 MkDocs Material로 빌드해 GitHub Pages(`gh-pages` 브랜치)로 발행한다.
라이브: https://choonchoon-ai.github.io/choonbrain/ · 그래프: `/graph/`.

wiki를 바꾸는 작업(Ingest·Archive·자동수정 Lint) 뒤에는 저장소 루트에서 재배포한다:

```bash
# 데스크톱(로컬 venv 있음):
.venv/bin/mkdocs gh-deploy --force

# mkdocs가 없으면(클라우드/웹 세션 등) 먼저 설치:
pip install -q mkdocs-material && mkdocs gh-deploy --force
```

- `mkdocs.yml`의 `docs_dir: wiki` 설정으로 wiki/ 전체가 사이트가 된다. `wiki/graph/`의 html·json도 그대로 복사된다.
- gh-deploy는 빌드 결과를 `gh-pages` 브랜치로 푸시한다(코드 변경 커밋은 별도로 main에 push).
- `.github/workflows/`는 gitignore되어 있다(현재 토큰에 workflow 권한 없음). 자동배포 대신 위 명령으로 발행한다.

## 공통 규약

- 표준 마크다운, 상대 링크 사용.
- wiki/ 주제 폴더는 **1단계까지만** 허용 (더 깊은 중첩 금지).
- 날짜: 로그·Collected·Archived는 오늘 날짜. Updated는 지식 내용이 바뀐 날. Published는 원본 발행일("Unknown" 허용).
- 링크 경로: 위키 파일 안에서는 현재 파일 기준 상대 경로, 대화 출력에서는 프로젝트 루트 기준 경로.
- 파일 쓰기 범위: Ingest는 index.md와 log.md 모두 갱신, Archive도 모두 갱신, Lint는 log.md(자동 수정 시에만 index.md), 일반 Query는 아무 파일도 쓰지 않는다.
